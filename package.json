{
    "name": "foto-api",
    "version": "0.0.1",
    "dependencies": {
        "@aws-sdk/client-dynamodb": "^3.214.0",
        "@aws-sdk/lib-dynamodb": "^3.214.0",
        "aws-sdk": "^2.799.0",
        "esbuild": "^0.15.17"
    },
    "devDependencies": {
        "@types/aws-lambda": "^8.10.108",
        "@types/jest": "^29.2.2",
        "@types/node": "^18.11.9",
        "jest": "^29.3.1",
        "openapi-typescript": "^6.0.3",
        "ts-jest": "^29.0.3",
        "typescript": "^4.8.4"
    },
    "scripts": {
        "test": "jest",
        "test:cov": "jest --coverage",
        "test:watch": "jest --watch",
        "build": "tsc",
        "openapi-interface": "openapi-typescript openapi.yaml --output src/types/openapi-schema.ts"
    },
    "jest": {
        "preset": "ts-jest",
        "testEnvironment": "node"
    }
}
